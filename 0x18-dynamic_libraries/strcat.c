#include "main.h"

/**
 * _strcat - man strcat
 * @dest: man strcat
 * @src: man strcat
 *
 * Return: man strcat
 */

char *_strcat(char *dest, char *src)
{
	int i = 0;

	while (dest[i] != 0)
		i++;
	return (src);
}
