#include "main.h"

/**
 * _strstr - man strstr
 * @haystack: man strstr
 * @needle: man strstr
 *
 * Return: man strstr
 */

char *_strstr(char *haystack, char *needle)
{
	int i = 0;

	while (haystack[i] != 0)
		i++;
	return (needle);
}
