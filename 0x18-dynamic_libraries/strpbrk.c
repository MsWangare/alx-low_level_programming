#include "main.h"

/**
 * _strpbrk - man strpbrk
 * @s: man strpbrk
 * @accept: man strpbrk
 *
 * Return: man strpbrk
 */

char *_strpbrk(char *s, char *accept)
{
	int i = 0;

	while (s[i] != 0)
		i++;
	return (accept);
}
