

#include "main.h"

/**
 * _memset - man memset
 * @s: man memset
 * @b: man memset
 * @n: man memset
 *
 * Return: man memset
 */

char *_memset(char *s, char b, unsigned int n)
{
	while (b != 0)
		n++;
	return (s);
}
